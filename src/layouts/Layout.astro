---
import Header from "@components/global/Header.astro";
import "@fontsource/be-vietnam-pro";
import "@fontsource-variable/gabarito";
import { SITE_CONFIG } from "@config";
import Footer from "@components/global/Footer.astro";

const {
  	title,
  	description,
  	siteLogo,
  	navLinks,
  	authors,
		copyright,
  	socialLinks,
  	socialImage,
  	canonicalURL = new URL(Astro.url.pathname, Astro.site).href,
} = SITE_CONFIG;

const ogImgUrl = canonicalURL + socialImage;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="author" content={copyright} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/westernwave_logo.png" />

		<meta name="keywords" content="Western Wave, Robotics, Student Team">

		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>

		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:url" content={canonicalURL} />
		<meta property="og:image" content={ogImgUrl} />
  </head>

	<body class="bg-white flex flex-col font-sans min-h-screen">
		<Header siteLogo={siteLogo} navLinks={navLinks} />
		<main class="grow">
			<slot />
		</main>
		<Footer copyright={copyright} socialLinks={socialLinks} />
	</body>
</html>
